-- _SetupCoreGui.client.lua
-- This script ensures that the core GUI elements are set up correctly at the start of the client session.

game:GetService("StarterGui"):SetCoreGuiEnabled(Enum.CoreGuiType.Backpack, true)
